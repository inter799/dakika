services:
  - type: web
    name: dakika
    env: python
    runtime: python3.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 wsgi:application
    plan: free
