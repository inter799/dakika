# Netlify Functions 依赖
# 核心依赖（必须）
Flask>=2.3.0
flask-cors>=4.0.0
python-docx>=0.8.11
openai>=1.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
duckduckgo-search>=8.0.0
supabase>=2.0.0

# PDF 解析
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Excel / PPT 解析
openpyxl>=3.1.0
python-pptx>=0.6.22

# Claude / Gemini SDK（可选，都在 5MB 以内）
anthropic>=0.40.0
google-generativeai>=0.8.0

# 注意：以下超大包无法在 Netlify（50MB限制）中安装
# Pillow（~40MB）— 图片处理，缺少时图片上传降级为 base64 传输
# pytesseract — OCR，缺少时图片文字识别不可用（还需 tesseract 二进制）
